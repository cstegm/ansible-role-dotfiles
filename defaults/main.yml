homepath: "{{ ansible_env.HOME}}" 
username: "{{ ansible_env.LOGNAME }}"
tools:
  - git
  - curl
  - wget
  - rsync
  - tmux
liquidprompt_repo: "https://github.com/nojhan/liquidprompt.git"
tmux_repo: "https://github.com/gpakosz/.tmux.git"
